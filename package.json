{
  "name": "types-google-protobuf",
  "version": "0.1.0",
  "types": "index.d.ts",
  "scripts": {
    "lint": "tslint --fix *.d.ts *.ts",
    "dist": "mkdir -p dist && cp index.d.ts dist/ && cp google-protobuf-tests.ts dist/ && cp tsconfig.json dist/ && cp tslint.json dist/"
  },
  "author": "Marcus Longmuir",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^4.5.1",
    "typescript": "^2.2.1"
  }
}
