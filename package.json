{
  "name": "types-google-protobuf",
  "version": "0.1.0",
  "types": "index.d.ts",
  "scripts": {
    "lint": "tslint --fix *.d.ts *.ts",
    "format": "./format.sh",
    "dist": "rm -rf dist && mkdir -p dist/google && cp index.d.ts dist/ && cp -R google/ dist/google && cp google-protobuf-tests.ts dist/ && cp tsconfig.json dist/ && cp dt-tslint.json dist/tslint.json"
  },
  "author": "Marcus Longmuir",
  "license": "MIT",
  "devDependencies": {
    "tslint": "^4.5.1",
    "typescript": "^2.2.1"
  }
}
